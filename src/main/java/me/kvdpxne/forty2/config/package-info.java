/**
 * Provides predefined sets of characters commonly used for generating NanoID identifiers.
 * <p>
 * This package contains utility classes that offer standard character dictionaries (e.g., uppercase
 * letters, lowercase letters, numbers, alphanumeric) as well as specialized sets for specific use
 * cases like cookie-safe strings or human-readable IDs. These dictionaries are defined as
 * {@code byte[]} arrays for efficient use with the NanoID generation algorithm, assuming ASCII
 * encoding.
 * </p>
 *
 * @since 0.2.0
 */
package me.kvdpxne.forty2.config;